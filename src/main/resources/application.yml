spring:
  profiles:
    active: pglocal
  jpa:
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: create
    show-sql: true

logging:
  level:
    org.springframework.security: DEBUG